language: node_js
node_js:
- '8'
install:
- npm install
script:
- npm run test
- npm run storybook-publish

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GIT_API_KEY
  local_dir: storybuild
  target_branch: gh-pages
  on:
    branch: feat/gh-storybook #TODO: update to master
